/**
 *
 * @author
 *
 */
var ImageInfo = (function (_super) {
    __extends(ImageInfo, _super);
    function ImageInfo(url, name, desc) {
        _super.call(this, url, name, desc);
    }
    var d = __define,c=ImageInfo;p=c.prototype;
    return ImageInfo;
})(FileInfoBase);
egret.registerClass(ImageInfo,"ImageInfo");
